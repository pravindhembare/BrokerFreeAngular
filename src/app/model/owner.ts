export class Owner {
    oid? : number;
	oname ?: string;
	oemail ? : string;
	ocontact ? : string;
    oaddress ? : string;
    opassans ? : string;
    opassreq ? : string;
    opassword ?: string;
    ocpassword ? : string;
    photo ? : string;
   constructor()
    {
       

    }
}
